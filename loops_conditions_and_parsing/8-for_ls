#!/usr/bin/env bash
# This script lists non-hidden files and displays the part after the first dash
for file in *; do
  # Skip hidden files (defensive; * normally excludes dotfiles)
  [[ "$file" == .* ]] && continue
  echo "${file#*-}"
done

