#!/usr/bin/env bash
# Displays PID and process name for processes whose name contains "bash"
for dir in /proc/[0-9]*; do
  pid="${dir#/proc/}"
  comm="$(cat "$dir/comm" 2>/dev/null)"
  if [[ "$comm" == *bash* ]]; then
    echo "$pid $comm"
  fi
done

